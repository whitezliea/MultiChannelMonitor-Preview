<Window x:Class="Presentation.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MultiChannel Monitor" Height="780" Width="1240"
        MinHeight="680" MinWidth="1080"
        Background="{StaticResource PageBackgroundBrush}">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="18,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="MultiChannel Monitor" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="{Binding HeaderSubtitle}" Foreground="{StaticResource TextMutedBrush}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Device: MCMD-001" Margin="0,0,16,0" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBlock Text="{Binding DataSourceStatus}" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" />
                </StackPanel>
            </Grid>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="18,8">
            <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left" Text="{Binding StatusText}" Foreground="{StaticResource TextMutedBrush}" />
                <TextBlock DockPanel.Dock="Right" Text="{Binding ClockText}" Foreground="{StaticResource TextMutedBrush}" />
            </DockPanel>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#17202A" Padding="14">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Top">
                        <TextBlock Text="MONITOR" Foreground="#D7E1EA" FontWeight="SemiBold" FontSize="18" Margin="2,0,0,18" />
                        <ItemsControl ItemsSource="{Binding NavigationItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            HorizontalContentAlignment="Left"
                                            Margin="0,0,0,8"
                                            Background="#22303D"
                                            Foreground="White"
                                            BorderBrush="#34495E">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Glyph}" Width="24" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Title}" />
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Bottom">
                        <Button Content="Start Acquisition" Command="{Binding StartCommand}" Margin="0,0,0,8" />
                        <Button Content="Stop Acquisition" Command="{Binding StopCommand}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding CurrentPageTitle}" Style="{StaticResource PageTitleStyle}" Margin="0,0,0,14" />
                <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" />
            </Grid>
        </Grid>
    </DockPanel>
</Window>
